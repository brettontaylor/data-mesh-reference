node_modules/
*.log
.DS_Store
.env
.env.local

# generated/ IS committed on purpose — it lets readers inspect the output
# without running the toolkit. Regenerate with `npm run generate`.
# Local medallion run outputs are noisy, so ignore just those:
generated/medallion/
