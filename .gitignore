node_modules/
*.log
.DS_Store
.env
.env.local
dist/
# Exception: the ERD module ships a PRE-BUNDLED artifact that MUST be deployed
# (host/Railway builds do not rebuild it). Un-ignore the dir then its files so
# `railway up` (which applies .gitignore patterns to the upload) includes them.
!apps/web/erd/dist/
!apps/web/erd/dist/erd.js
!apps/web/erd/dist/erd.d.ts
.turbo/
.next/
*.tsbuildinfo
next-env.d.ts

# Engine generated/ IS committed on purpose — it lets readers inspect the output
# (and the site vendors it). Regenerate with `pnpm generate`.
# Local medallion run outputs are noisy, so ignore just those:
packages/engine/generated/medallion/
