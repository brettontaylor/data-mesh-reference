{
  "name": "@dct/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/server.ts",
  "description": "Mapping and Metadata Platform control-plane API (Phase 1: read surfaces over the projection).",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "reconcile": "tsx src/reconcile-cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@dct/audit": "workspace:*",
    "@dct/auth": "workspace:*",
    "@dct/catalog-adapter": "workspace:*",
    "@dct/engine": "workspace:*",
    "@dct/events": "workspace:*",
    "@dct/git-adapter": "workspace:*",
    "@dct/orchestration-adapter": "workspace:*",
    "@dct/projection": "workspace:*",
    "@dct/shared": "workspace:*",
    "fastify": "^5.2.0",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
