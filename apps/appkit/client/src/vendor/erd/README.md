# Vendored ERD bundle (do not edit)

Pre-built artifact copied from `apps/web/erd/dist/` — the interactive data-model
explorer (React Flow + layered layout inlined; only react/react-dom external).
To update: rebuild in `apps/web/erd` (see its README) and re-copy both files.
